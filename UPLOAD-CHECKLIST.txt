ADios — GitHub upload checklist (AlexRabbit)
============================================

Upload ONLY these files/folders.

REQUIRED — launchers & docs
  ADios.bat
  ADios.sh
  README.md
  LICENSE
  .gitignore

REQUIRED — build engine
  config/build_hosts.py
  config/lists
  config/blacklist
  config/whitelist
  config/remover          <- dead/expired domains (ONLY remover file)
  config/probe_cache

REQUIRED — blocklist outputs
  hosts
  pihole-hosts
  dnscrypt-hosts
  adguardhosts.txt

REQUIRED — auto-update
  .github/workflows/update-hosts.yml

DO NOT UPLOAD
  remover.txt             <- removed; use config/remover only
  .git/
  __pycache__/
  .cursor/
  *.log

Author: AlexRabbit
